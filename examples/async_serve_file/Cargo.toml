[package]
name = "haproxy_async_module"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
haproxy-api = { path = "../.." }
mlua = { version = "0.9", features = ["macros"] }
tokio = { version = "1.32", features = ["full"] }
bstr = "1.0"
